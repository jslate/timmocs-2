%img{ src: 'images/branch_moved_forward.svg' }

= text 'As new timmocs are created on a branch, the pointer just moves forward.'

#code-container
  = cmd '[sad]: git log --oneline --decorate'
  = diff_line '9f7cad2 (HEAD -> sad) add sad mouth'
  = diff_line 'ff4d610 add nose'
  = diff_line '141170e initial commit'
